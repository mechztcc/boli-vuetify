export interface IFilterState {
  city: string;
  credit: number;
  day: string;
  district: string;
  level: number[];
  modality: number[];
  local: boolean;
  online: boolean;
  teacherName: string;
  weekDay: string[];
}
